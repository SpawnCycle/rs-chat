use std::time::Duration;

pub const TICK_DURATION: Duration = Duration::from_millis(100);
pub const CHANNEL_BUFFER_SIZE: usize = 128;
pub const WS_TIMEOUT_DURATION: Duration = Duration::from_secs(3);
